# =============================================================================
# cc-connect 多 agent 微信桥接 · 配置样例（已脱敏）
# =============================================================================
#
# 作用：把三个不同的 AI agent（Claude Code / DeepSeek Harness / WorkBuddy）
#       接到同一个微信机器人入口，共用一个微信号与用户对话。
#
# 使用方法：
#   1. 复制到 ~/.cc-connect/config.toml
#   2. 替换所有 <尖括号> 占位符
#   3. cc-connect daemon stop   # 注意：本工具 stop 常不真正杀进程，见 docs/04
#      cc-connect daemon start
#
# ⚠️ 核心前提（务必先读 docs/05-能力边界实测.md）：
#   **原生** cc-connect 是扇出，不是路由：三个 project 共用同一个微信 bot token 时，
#   一条用户消息会被**原样投给每个 project**，「谁该回答」只能靠 agent 自己读消息后判断。
#   → 本仓库的补丁在引擎层加了**点名关卡（L1）**，把「@ 了谁」变成真正的路由：
#     命中别人的消息**在进入 agent 之前**就被丢掉（不建会话、不起 agent 进程、不花 token）。
#     它需要套用 patches/0007 + 0008；**没套补丁时行为仍是纯扇出**。
#   → 三道防线（L1 关卡 / L2 前置钩子 / L3 输出静默）的分工、边界与实测见
#     docs/07-点名路由与三层防线-落地报告.md。
# =============================================================================


# -----------------------------------------------------------------------------
# 全局：agent 之间的跨项目拨号（信使）
# -----------------------------------------------------------------------------
# 让 A agent 能主动找 B agent，且 B 不需要在线 —— cc-connect 会把消息转过去并等回复。
[relay]
timeout_secs = 120
# visibility 只控制「群聊里的额外回声」；私聊（dm）场景下是空操作。
visibility = "full"


# -----------------------------------------------------------------------------
# 全局：只发正式回复，不把思考过程/工具进度当作独立消息发出
# -----------------------------------------------------------------------------
# 必要性：部分 agent（尤其带推理模式的）会把思考文字混进回复正文，
#         导致「整段恰好等于 NO_REPLY」的静默判定失效、思考被真的发到微信。
[display]
thinking_messages = false
tool_messages = false
# agent_label：给**每条出站消息**加来源前缀，让共用同一个微信窗口的多个 agent 可分辨。
# 用 {label} 占位，替换成该 project 的 label（没设 label 则用 name）。
#   全局这里配  = 所有 project 都带前缀
#   单个 project 可在 [projects.display] 里覆盖
#   两处都不配  = 不加前缀
# 施加点在引擎的**两个发送咽喉**，所以回复、报错、排队提示都会带上；
# 长文本被平台切成多条时**只有第一块**带前缀（标识的是「我是谁」，不需要每条重复）。
agent_label = "[{label}] "


# -----------------------------------------------------------------------------
# 全局：点名路由（L1 —— 引擎层关卡）
# -----------------------------------------------------------------------------
# 作用：让「消息里**第一个** @ 记号指向谁」决定这条消息进哪个 project，
#       而且是在**进入 agent 之前**判定。命中别人的消息在引擎里当场丢掉 ——
#       不建会话、不起 agent 进程、不花 token。
#
# 判定规则：
#   · 第一个 @ 指向**别的** project → 丢弃
#   · 第一个 @ 指向**自己**         → 放行
#   · 没有 @                        → 放行（是否回答由提示词层的 NO_REPLY 约定兜底）
#
# 别名归一化：大小写、首尾空白、前导 @ 都可省 —— "@DSH" / " @dsh " / "dsh" 是同一个记号。
# 匹配按**最长别名前缀**，所以同一个 project 登记 @d 与 @dsh 时，@dsh 不会被 @d 遮住。
#
# ⚠️ 别写错：project 必须能对上某个 [[projects]].name；同一个 project 只能声明一次；
#    同一个别名不能被两个 project 认领；每个条目至少要给一个非空别名 —— 这些在配置
#    加载阶段就会被拒绝，不会拖到运行期变成诡异的投递行为。
#
# 与另外两层的关系（**三层叠加，不是替代**）：见 docs/07-点名路由与三层防线-落地报告.md
[[mention_agents]]
project = "my-project"
aliases = ["@claude"]
# default 决定「没有点名任何人」的消息在这个 project 里的默认动作：
#   "answer" —— 照常回答（适合主 agent / 唯一 agent）
#   "silent" —— 静默（适合「只被点名时才回答」的 agent）
# 不写则按实现默认走，**建议显式写清**，否则日后很难判断行为来自配置还是默认值。
default = "answer"

[[mention_agents]]
project = "my-dsh"
aliases = ["@dsh"]
default = "silent"

[[mention_agents]]
project = "my-workbuddy"
aliases = ["@wb", "@workbuddy"]
default = "silent"
# media_default：微信无法在一条消息里同时发文字和图片（三种绕法都实测失败，
# 见 docs/08），所以「@dsh」和图片**必然分两条到达**，第二条不含任何点名信息。
# 本条指定「谁来认领这种没头没主的媒体消息」。
#   · 整个配置里**只应有一个** project 设 true，建议给额度最充裕的那个；
#   · 它只在「当前 session 还没有被点名对象」时生效 —— 先点了名再发图，图仍归被点名者
#     （粘性点名，TTL 3 分钟；且只对**无文字**消息生效，不会污染文字路由）。
media_default = true
# enabled（可选，布尔）：显式关掉某个 project 的关卡。不写 = 按 aliases 是否为空判定。
# 关卡是「按 project 各配一份」，不是全局开关 —— 某个 project 不配就不受关卡保护，
# 它会重新变成「所有消息都进、全靠 NO_REPLY 闭嘴」的旧行为。


# =============================================================================
# Project 1：Claude Code（type = "claudecode"）
# =============================================================================
[[projects]]
name = "my-project"
# label：该 project 的显示名，供上面的 agent_label 出站前缀使用（不设则退回用 name）。
label = "Claude"
# ⚠️ admin_from 必须是**字符串**。写成 TOML 表（[projects.admin_from]）会让整个
#    配置解析失败、服务起不来，且日志只有反复的 instance lock，极难定位。
admin_from = "<YOUR_OPENID>@im.wechat"

[projects.agent]
type = "claudecode"

[projects.agent.options]
work_dir = "C:/Users/<YOU>"
# mode 必须显式设为 bypassPermissions：
#   "default" 的语义是「每次工具调用都要人工确认」，而桥接是无值守的 ——
#   agent 发出权限请求后没人点「允许」，回合就永久挂起，微信侧表现为「完全没回」。
#   风险边界 = 本 project 平台配置里的 allow_from 白名单，请勿外借账号。
mode = "bypassPermissions"
# 点名制提示词：cc-connect 检测到回复整体等于 NO_REPLY 时判为静默、不发出。
# 注意这是**提示词工程**，不是桥接的内置开关（NO_REPLY 这个字面量在二进制里搜不到）。
append_system_prompt = '这是一个「微信多 agent 桥接」会话，可能存在多个 AI 同时收到同一条消息。路由规则：1) 若用户消息以 @dsh 开头（点名 DeepSeek Harness），你什么都不要回答，只输出一行 NO_REPLY（不要有任何其他文字、标点或说明）；2) 若消息以 @claude 开头，去掉这个前缀后正常作答；3) 消息没有 @ 前缀时，正常作答。'

[[projects.platforms]]
type = "weixin"

[projects.platforms.options]
token = "<YOUR_BOT_TOKEN>"
base_url = "https://ilinkai.weixin.qq.com"
account_id = "<YOUR_BOT_ACCOUNT_ID>"
# allow_from 的语义是「**谁**可以给这个 project 发消息」，按发送者过滤。
# 把它设成某个不存在的 ID，就等于给该 project 装了一个「入站总开关」（关）。
allow_from = "<YOUR_OPENID>@im.wechat"
clean_reply = true


# =============================================================================
# Project 2：DeepSeek Harness（type = "acp"）
# =============================================================================
# DSH 自带标准 ACP server：dsh --profile acp（automation-only JSON-RPC stdio）
[[projects]]
name = "my-dsh"
label = "DSH"

[projects.agent]
type = "acp"

[projects.agent.options]
work_dir = "C:/Users/<YOU>"
command = "<NODE_EXE_PATH>"
# args 结构 = [中间层脚本, node, agent 入口, ...agent 参数]
# 中间层 acp-thought-filter.js 的作用：丢弃 agent_thought_chunk（思考块）。
# 原因：cc-connect 的 ACP 适配器不区分「思考块」与「正文块」，会把两者拼成同一条
#       回复，于是「整段恰好等于 NO_REPLY」的静默判定失效，思考独白被发到微信。
args = ["<REPO>/scripts/acp-thought-filter.js", "<NODE_EXE_PATH>", "<DSH_ENTRY>/lib/bin.js", "--profile", "acp"]
display_name = "DSH"

[[projects.platforms]]
type = "weixin"

[projects.platforms.options]
token = "<YOUR_BOT_TOKEN>"
base_url = "https://ilinkai.weixin.qq.com"
account_id = "<YOUR_BOT_ACCOUNT_ID>"
allow_from = "<YOUR_OPENID>@im.wechat"
clean_reply = true


# =============================================================================
# Project 3：WorkBuddy（type = "acp"，走它内置的 CodeBuddy CLI）
# =============================================================================
# 原理：WorkBuddy 桌面版本体就是 CodeBuddy CLI，支持标准 ACP
#       （--acp --acp-transport stdio），可直接对接。
# 必须用 --acp：CLI 的 -p / --serve 路径会绑定一个按机器固定的端口（会被桌面版
#              会话占用）并永久挂起；--acp 不绑该端口。
[[projects]]
name = "my-workbuddy"
label = "WorkBuddy"

[projects.agent]
type = "acp"

[projects.agent.options]
# 用独立目录，避免读到家目录下给其他 agent 写的 CLAUDE.md 规则（否则会串规则）
work_dir = "C:/Users/<YOU>/wb-agent"
command = "<NODE_EXE_PATH>"
args = ["<REPO>/scripts/acp-thought-filter.js", "<NODE_EXE_PATH>", "<CODEBUDDY_ENTRY>", "--acp", "--acp-transport", "stdio", "--permission-mode", "bypassPermissions", "--mcp-config", '{"mcpServers":{}}']
display_name = "WorkBuddy"

[[projects.platforms]]
type = "weixin"

[projects.platforms.options]
token = "<YOUR_BOT_TOKEN>"
base_url = "https://ilinkai.weixin.qq.com"
account_id = "<YOUR_BOT_ACCOUNT_ID>"
allow_from = "<YOUR_OPENID>@im.wechat"
clean_reply = true
